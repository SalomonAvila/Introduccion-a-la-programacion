#include <iostream>

using namespace std;

void revision(int c, int m, int n, int referencia, int escalones[]){
    int diferencia = abs(referencia,escalones[0]);
    int minima = diferencia;
    int maxima = diferencia;
}

int main(){
    int c,m,n; cin>>c>>m>>n;
    int referencia; cin>>referencia;
    int escalones[n];
    for(int i = 0; i<n; i++){
        cin>>escalones[i];
    }
}